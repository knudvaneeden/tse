1. -To install

     1. -Take the file grep3230_knud.zip

     2. -And unzip it in any arbitrary directory

     3. -Then run the file zipinstallgrep3230.bat

     4. -That will create a new file grep3230_knud.zip
         in that directory

     5. -Unzip that latest file in a new arbitrary directory

     6. -Then run the file

          grep.mac

2. -The .ini file is the local file 'grep3230.ini'
    (thus not using tse.ini)
