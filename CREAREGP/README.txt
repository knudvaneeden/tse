crearegp

Use case = record: create: file: version: control: github: project
1. -To install

     1. -Take the file crearegpknud.zip

     2. -And unzip it in any arbitrary directory

     3. -Then run the file zipinstallcrearegp.bat

     4. -That will create a new file crearegp_knud.zip
         in that directory

     5. -Unzip that latest file in a new arbitrary directory

     6. -Then run the file

          crearegp.mac

2. -The .ini file is the local file 'crearegp.ini'
    (thus not using the global file 'tse.ini')
