; MACRO initialization/configuration companion file
; Version: 1.0.0.0.1
; Date: 2026-09-20
; Time: 21:44 CEST
;
; IMPORTANT:
; The supplied historical MACRO.MAC is compiled and its source code is not
; included. The original package does not document INI-file support.
; These values therefore serve as package defaults/documentation for now
; and are not assumed to be read automatically by MACRO.MAC.
;
; Empty directory values mean: use the original/default TSE location.

macro=MACRO.MAC
data=MACRO.DAT
query=MACRO.QRY
settings=MACRO.SET

; Optional directories. Leave empty for original/default behavior.
tseprogramdir=
macropath=
